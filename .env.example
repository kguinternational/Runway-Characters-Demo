# Server-only. Never add NEXT_PUBLIC_ to this name.
RUNWAYML_API_SECRET=

# Written automatically by `pnpm convex dev`.
NEXT_PUBLIC_CONVEX_URL=
CONVEX_DEPLOYMENT=

# Optional server-only URL for the backend tool handler.
CONVEX_URL=
